# 개선/보완 백로그

자동 사이클이 매번 위에서부터 열린(`[ ]`) 항목 하나를 골라 처리하고, 새로 발견한 문제는
맨 아래에 추가한다. 처리 완료 시 "처리 완료" 섹션으로 옮기고 한 줄로 조치 내용을 남긴다
(자세한 내용은 docs/DEVLOG.md에 기록).

## 열린 이슈
- [ ] "추천 코스" 개념이 GuideContent(contentType='tip')로 임시 표현되어 있음 — 코스가 여러
      장소를 순서대로 묶는 구조를 제대로 표현하려면 별도 Prisma 모델(예: CourseTemplate +
      CourseTemplateStop)이 필요할 수 있음. 스키마 마이그레이션이라 사용자 확인 후 진행할 것.
- [ ] Place 이름 표기 차이(예: "오사카성" vs "오사카 성")로 인한 중복 적재 가능성 — 저장 전
      유사 이름 체크/정규화 로직 필요
- [ ] scripts/collect.mjs에 대한 자동 테스트 없음 (수동 검증만 함)
- [ ] app/discover 페이지의 항공/숙소 검색은 항상 실시간 호출 — 캐싱 없음 (키 연동 후 비용/
      속도 이슈 가능성)

## 처리 완료
(아직 없음)
